# P0 Product Usability Implementation Plan

1. Add failing contracts for durable training response validation and immediate `TrainingTaskRuntime` merge; implement and commit.
2. Add failing navigation owner tests; register `ServiceNodeRuntime` and remove title-observer routing; implement and commit.
3. Add failing immediate-shell and stale-guard tests; implement parallel hydration and commit.
4. Add failing annotation API/render tests; batch-load formal annotations, add SVG overlays, and commit.
5. Run the focused tests and four browser smoke paths, then update the current handoff/state documentation with evidence.
