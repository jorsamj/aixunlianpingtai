# 42.25 实施交接与进度账本

> 本文件是 `feat/42.25.0` 的长期交接入口。ChatGPT、Codex 或人工继续开发前，先读本文件，再读 `docs/architecture/42.25-authoritative-paths.md` 和当前 `git log`。
>
> 规则：只记录已经从代码或实际 CI 确认的状态；服务器/真实硬件未验证项必须明确写“未验证”。不要仅依据 commit 标题把任务标为完成。

## 当前基线

- 分支：`feat/42.25.0`
- 基于 42.24.0：`01636b61780361dde91c12ac2732f46d2289b6be`
- Codex 中断现场：`99608d2 wip: preserve interrupted 42.25 work`
- 本文件更新前分支 HEAD：`7961593064c12ce2d74d0aa969c42b0d06d91775`
- `main` 暂不合并；先完成剩余 P0、定向 CI 和 Linux/A800 真实验收。

## 已从代码确认的核心能力

- 训练 bundle 使用隔离真实复制，不再 hardlink/symlink 源素材；包含源 SHA、复制后 size/SHA、原子发布和 link/reparse 防护。
- Stable Label ID 已落地：项目标签保留稳定 `label_id`，训练任务冻结 `label_id -> task-local yolo_class_id`。
- `annotation_state` / `annotation_scope` 已用于区分正样本、合法 scoped negative 与 GT 不完整素材。
- Test sealing 已落地：训练工作区仅物化 Train/Validation；best 模型确定后才单独物化 Test 做 Final Evaluation。
- Worker authoritative preflight 已覆盖冻结标签、split、SHA、annotation、设备、磁盘、RAM/SHM 等检查。
- GPU reservation、训练配置贯穿、训练指标、Worker/子进程身份恢复、post-processing 恢复已有实现。
- `current_version_id` / rollback / version pointer audit 已接入真实训练基线。
- `algorithms.json` 新 mutation path 已有跨进程文件锁、revision/CAS 和同 task_id version attach 幂等。
- `task_worker.py --roles all` 已改为 supervisor + 独立 role worker，长导入不再天然串行阻塞训练/转换。
- 部署测试子进程已使用 durable process identity、heartbeat、完整进程树取消和 recovery fencing。
- 新训练版本已冻结 Artifact Manifest，并在后续迭代前重新校验。
- Generic task retry 已改成新 task identity，旧终态记录和错误信息保留，不再覆盖历史。

## 已收口 P0

### P0-01 Duplicate / Leakage Component

`platform_core/training_splits.py` 在随机切分前按 `content_sha256`、来源 group / 视频 group、file identity 建立 leakage component：

- exact duplicate + 本次 selected-label GT 相同：稳定 canonical dedupe；
- exact duplicate + 本次 selected-label GT 不同：切分前 fail-closed；
- independent Train/Test 若 SHA、来源组或 file identity 落入同一 component：直接阻断；
- 最终 `_assert_no_leakage()` 继续作为最后防线。

`training_labels.py` 又把 duplicate GT fencing 前移到 durable TRAINING task 创建前，因此不必等 Worker 启动后才看到同类冲突。

GitHub Actions Windows/Ubuntu 定向测试已通过。真实 10k/20k 数据规模性能仍需 A800 服务器验收。

### P0-02 Empty boxes 不得自动变 confirmed_empty

`platform_core/annotation_repository.py` 默认规则已改为：有 boxes -> `annotated`；无 boxes 且没有显式状态 -> `unannotated`。

只有人工确认、可信导入等显式路径才能写 `confirmed_empty`，且需要正确 annotation scope。定向测试已纳入 Windows/Ubuntu P0 CI。

### P0-03 Rollback 指针接入真实训练基线

`TrainingHandler` 调 `choose_iteration_base()` 时显式传入 `algorithm['current_version_id']`。回滚 V5 -> V3 后，下一次迭代必须从 V3 的 verified train checkpoint 开始；当前版本不可训练时返回 `CURRENT_VERSION_UNAVAILABLE`，不会静默切换到 V5、其他历史版本或母模型。

### P0-04 algorithms.json 跨进程 mutation fence

`platform_core/algorithms.py` 已加入 Windows/POSIX 跨进程 file lock、原子写、per-algorithm revision、expected_revision 冲突检测、同 task_id version attach 幂等，以及只移动 current pointer 的 rollback。

长期仍建议将算法版本元数据迁入事务型数据库，但当前 JSON 路径已具备基本多 Worker 写安全。

### P0-05 Training Version Artifact Manifest

新训练版本来自标准 task artifact 目录时，`attach_version()` 会从该训练任务的 `snapshot.json` / `payload.json` 建立冻结 manifest：

- 模型文件名、size、SHA256；
- framework / task type / input size；
- snapshot_id / parent_version_id；
- source model / parent model SHA lineage；
- num_classes / names；
- stable `label_id`、业务 code、platform class id、task-local YOLO class id；
- manifest 自身 SHA256。

严格迭代选择会校验 manifest 自身 hash、内部标签结构、模型 size/SHA、snapshot/parent 对应关系和父模型 hash lineage。模型被替换、manifest 被篡改、父版本 manifest 消失或标签快照错配均 fail-closed。

`tests/unit/test_algorithm_artifact_manifest.py` 已覆盖 manifest 生成、模型篡改、自身 hash 篡改、snapshot 错配、payload/snapshot label mapping 错配、parent model hash lineage、parent manifest 丢失、idempotent replay 和 replay 后模型损坏。

这些测试已进入 P0 workflow；Windows/Ubuntu 定向 CI 均有成功运行记录。真实 A800 训练产生的 best.pt manifest 一致性仍需服务器验收。

### P0-06 Generic Retry Attempt Audit

原 `TaskRepository.retry()` 会直接把 FAILED/CANCELLED 等旧任务行改回 QUEUED，覆盖原错误、结束时间和执行历史。当前运行时通过 `platform_core/task_runtime/audited_repository.py` 导出的 `TaskRepository` 改为：

- generic retry 创建新的 task_id；
- `retry_of` 指向紧邻的上一 attempt；
- 原 terminal task 行保持不变；
- 原 payload 复制到新 task 的独立 artifact namespace；
- 双击 Retry 时，如果已经存在活动 child attempt，返回同一个 child，不产生并行 sibling；
- 若旧祖先已有结束的 retry child，则要求从最新 attempt 继续 retry，保持线性审计链；
- `POST_PROCESSING_FAILED` 不走 generic retry，继续使用专门的 `retry_post_processing()`，避免重复训练；
- `MATERIAL_BATCH` retry 使用 SQLite backup 克隆冻结的 `selection.sqlite3`，保留 succeeded/failed 状态。新 attempt 的 MaterialBatchHandler 只把 failed rows 重新置 pending，因此不会重复处理已经成功的素材；
- payload 或 MaterialBatch 冻结 selection 缺失时 fail-closed，不创建注定无法执行的新任务。

旧 `tests/unit/task_runtime/test_repository.py` 中“retry 复用同 task_id”的过时断言已同步改为 immutable attempt 语义，并与新增 `tests/unit/task_runtime/test_retry_audit.py` 一同纳入 P0 workflow。

GitHub Actions run `34476902150`（HEAD `7961593`）中 Ubuntu 3.12 与 Windows 3.12 的 compile 和 targeted P0 tests 均已完成 `success`。因此本项在代码/CI 层可标记收口；真实长任务 crash/retry 的 Linux 端到端行为仍需服务器验收。

## 其他已收口项

### Worker 角色隔离

旧 `task_worker.py --roles all` 会让 import / annotation / video / training / conversion 等长任务共享一个串行 Scheduler。当前实现改为 supervisor 监督多个 role worker，各角色拥有独立执行通道。

子 Worker 使用统一 process identity（PID、create_time、command hash、process group、launch token）管理；父进程退出时使用 `ProcessController` 安全终止对应进程树。相关 Windows/Ubuntu CI 已通过。

### Deployment Test 子进程生命周期

部署测试已从裸 `subprocess.Popen()` 收口到 durable process identity：启动后绑定进程身份、运行中 heartbeat、Cancel 终止完整进程树、lease/DB 异常不遗留无主推理进程、recovery 只处理身份验证通过的旧进程；已有且验证通过的结果可以恢复提交而不是盲目重跑。

`.rknn/.om/.bmodel` 无真实板卡/runtime 时仍明确标记硬件阻断，不能把“转换成功”和“板端验证成功”混为一谈。

## 当前仍未闭环的 P0 / P1

- 检查是否存在绕过 `platform_core.task_runtime` 包导出、直接 import 旧 `repository.TaskRepository` 的生产路径；若存在，应迁移或把 audited retry 收回核心 repository，避免双语义。
- Material labels 与 Annotation GT 是否仍存在双真相 mutation path，需要继续清理为唯一 authoritative GT。
- AI 阶段评测/补料旧路径是否仍可能修改 immutable snapshot，或继续引用旧 runtime YAML，需要继续审计。
- Conversion Worker 的完整进程树、恢复、取消以及“模型转换成功 / 板端待验证”仍需端到端核对。
- API Key SecretStore、CORS、SSRF 防护仍未整体收口。
- ZIP/YOLO 10k/50k/100k 导入仍需验证：真正 worker 化、一次扫描、批量 SQL、避免重复 SHA/解码。
- source/cache/bundle/runs/artifacts/conversions 的全局 quota、retention、GC 尚未完成。
- MaterialRepository `%LIKE%`、JSON extract、全量读取和 monkey-patch 技术债仍需处理。
- AnnotationRepository 与 Material projection 的原子性 / reconcile 仍需继续审计。
- `_select_grouped()` subset-sum 在大量单元素 group 时的 CPU 复杂度仍需 10k/20k 真实测试，100k 前必须确认不退化。

## 当前 P0 CI 最小集

```powershell
python -m py_compile task_worker.py platform_core/task_runtime/repository.py platform_core/task_runtime/audited_repository.py platform_core/training_splits.py platform_core/training_labels.py platform_core/training_preflight.py platform_core/annotation_repository.py platform_core/algorithms.py platform_core/training_tasks.py platform_core/deployment/conversion_tasks.py platform_core/deployment/inference_tasks.py
python -m pytest tests/unit/task_runtime/test_repository.py tests/unit/task_runtime/test_retry_audit.py tests/unit/test_training_splits.py tests/unit/test_training_labels.py tests/unit/test_task_worker.py tests/unit/test_annotation_repository.py tests/unit/test_algorithms.py tests/unit/test_algorithm_artifact_manifest.py tests/unit/test_conversion_tasks.py tests/unit/test_deployment_inference_tasks.py -q
```

如果定向失败，先修直接相关失败，不无目的扩大到全量测试。

## 真实 Linux/A800 验收边界

GitHub Actions 只能验证代码路径，不等于生产验收。真实 A800 至少还要验证：

- `actual_device` 真正为 scheduler 分配的 A800；
- RAM/SHM guard 在真实 DataLoader worker 数下有效；
- 训练前后源素材 SHA 不变化；
- Train/Validation 运行期间 Test 未物化；
- best 固定后才访问 Test；
- post-processing crash/restart 不会重复训练；
- rollback 后下一次训练实际加载被回滚版本；
- Artifact Manifest 中模型 SHA 与真实 best.pt 一致；
- generic retry 创建独立 task_id，旧失败行、日志、错误和结束时间不被覆盖；
- MATERIAL_BATCH retry 只重试 failed rows；
- 10k/20k 真实素材下 split、bundle、任务刷新和导入耗时可接受。

100k 做有界架构验证；百万级未压测前保持“未验证”。

## 下一步优先级

```text
1. 审计 TaskRepository 的直接 import 路径，消除 audited/core 双语义风险
2. Material labels vs Annotation GT 双真相
3. AI 补料 / immutable snapshot / runtime YAML 旧路径
4. Conversion 完整生命周期与状态语义
5. API security：SecretStore / CORS / SSRF
6. ZIP/YOLO 大规模导入性能与存储 quota/retention/GC
7. Linux/A800 真实验收
```

Codex 恢复后不要重新从 42.24 开始分析。先 `git fetch origin && git switch feat/42.25.0 && git pull --ff-only`，阅读本文件和 `42.25-authoritative-paths.md`，核对当前 Actions/git log 后从上述下一步继续。
