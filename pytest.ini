[pytest]
testpaths = tests
addopts = --import-mode=importlib
markers =
    hardware: requires explicitly configured vendor SDK and real conversion hardware/toolchain
    real_training: runs a real local training process and reloads the produced model artifact
    scale: runs the opt-in 100k/500k/1m SQLite material-index acceptance test
