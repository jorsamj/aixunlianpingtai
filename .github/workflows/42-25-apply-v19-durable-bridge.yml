name: Apply 42.25 v19 durable bridge

on:
  push:
    branches:
      - feat/42.25.0
    paths:
      - .github/workflows/42-25-apply-v19-durable-bridge.yml
      - scripts/apply_v19_durable_bridge_patch.py
      - scripts/apply_browser_bridge_strict_mapping.py

permissions:
  contents: write

jobs:
  patch-app:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/42.25.0
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install focused dependencies
        run: >-
          python -m pip install
          pytest psutil pyyaml pillow
          fastapi httpx python-multipart requests
          keyring filelock boto3 oss2 opencv-python-headless
      - name: Apply fail-closed compatibility patches
        run: |
          python scripts/apply_v19_durable_bridge_patch.py
          python scripts/apply_browser_bridge_strict_mapping.py
      - name: Compile bridge and app
        run: >-
          python -m py_compile
          app.py
          platform_core/storage/browser_v19_bridge.py
          platform_core/storage/browser_v19_entry.py
          platform_core/storage/optimized_import_tasks.py
      - name: Run browser bridge tests
        run: python -m pytest tests/unit/storage/test_browser_v19_bridge.py -q
      - name: Commit audited compatibility changes
        shell: bash
        run: |
          if git diff --quiet -- app.py platform_core/storage/browser_v19_bridge.py; then
            echo "compatibility bridge already patched"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py platform_core/storage/browser_v19_bridge.py
          git commit -m "fix: keep browser yolo mapping explicit"
          git push origin HEAD:feat/42.25.0
