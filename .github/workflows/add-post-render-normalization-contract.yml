name: Add Post Render Normalization Contract

on:
  push:
    branches:
      - refactor/frontend-runtime-stabilization
    paths:
      - tools/add_post_render_normalization_contract.py
      - .github/workflows/add-post-render-normalization-contract.yml

permissions:
  contents: write

jobs:
  contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install browser-test backend dependencies
        run: >-
          python -m pip install --disable-pip-version-check
          fastapi uvicorn python-multipart pillow pyyaml requests
          opencv-python-headless onnx onnxruntime onnxslim psutil keyring filelock boto3 oss2
      - name: Install frontend dependencies
        run: npm ci
      - name: Add browser behavior contract
        run: python tools/add_post_render_normalization_contract.py
      - name: Verify current V37 owners satisfy the contract
        run: >-
          npx playwright test tests/browser/navigation-stability.spec.mjs
          --grep "modal table wrapping and first-field focus survive normalization ownership"
      - name: Commit permanent browser contract
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tests/browser/navigation-stability.spec.mjs
          git commit -m 'test(ui): lock post-render normalization behavior'
          git push
